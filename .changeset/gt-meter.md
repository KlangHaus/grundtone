---
---

Add GTMeter — progress/meter atom with three variants (determinate / accent / bidirectional
threshold), collapsing taktart's three bar-languages into one. Lands on develop, not released here
(2.x publish-frozen). Empty changeset.
