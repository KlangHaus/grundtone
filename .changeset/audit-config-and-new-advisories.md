---
---

Dependency-configuration only: deletes the stale audit accepts, fixes lodash-es, js-yaml and nanoid
by override, and accepts the two unpatchable image-size advisories with reasoning. Empty changeset
per the "frozen 2.x packages" guard — no package contents change.
