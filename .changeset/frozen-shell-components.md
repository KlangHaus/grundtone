---
---

New shell / redesign components (GTAppShell, GTDrawer, GTConfirmDialog, GTCodeBlock) and the
@grundtone/mcp package land on develop but are intentionally NOT released here: the 2.x line is
publish-frozen until the main/develop reconciliation. Empty changeset per the "frozen 2.x packages"
guard — real release changesets get added when the freeze lifts. Component descriptions live in
their feat(vue) commits.
