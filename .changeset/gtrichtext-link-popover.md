---
---

GTRichText iteration 2: the window.prompt link flow is replaced by the designer-anatomy link popover
(focus-trapped, URL allow-list validation rejecting javascript:/data:, optional text, new-tab with
rel=noopener), a compact edit bubble on caret-in-link (open/edit/unlink), ⌘/Ctrl+K, and a
roving-tabindex toolbar. Lands on develop but is intentionally NOT released here — the 2.x line is
publish-frozen until the main/develop reconciliation (empty changeset).
