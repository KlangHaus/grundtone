---
---

eslint 10 migration: coordinated core + @eslint/js + @typescript-eslint/parser bump, root config
ignores the Expo playground, and five `no-useless-assignment` fixes in the design-system/vue
keyboard-nav behaviors (Slider, Tabs, Carousel). Tooling + behaviour-preserving internal cleanup
only — no runtime/API change and the 2.x line stays publish-frozen, so this is intentionally NOT
released (empty changeset).
