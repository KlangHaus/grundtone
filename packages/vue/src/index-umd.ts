/**
 * UMD entry point — bundles the full design-system CSS for CDN / legacy use.
 * ESM consumers should import from the main index.ts instead (auto CSS).
 */
import '@grundtone/design-system/dist/index.css';
export * from './index';
